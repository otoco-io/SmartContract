const OtoCorp = artifacts.require('OtoCorp');
const ERC20 = artifacts.require('@openzeppelin/contracts/ERC20.sol');

module.exports = async function (deployer, network) {
    // if (network === 'development'){
    //     const ERC20Deploy = await ERC20.deployed();
    //     await deployer.deploy(OtoCorp, ERC20Deploy.address);
    // }
}